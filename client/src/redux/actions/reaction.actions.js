import * as types from '../constants/reaction.constants';
import api from '../api';

const createReaction = () => async (dispatch) => {};

const updateReaction = () => async (dispatch) => {};

const deleteReaction = () => async (dispatch) => {};

export const reactionActions = {
  createReaction,
  updateReaction,
  deleteReaction,
};
